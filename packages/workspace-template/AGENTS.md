# AGENTS.md

本仓库默认以 `CLAUDE.md` 为主入口。

如果你的平台优先读取 `AGENTS.md`，则本文件与 `CLAUDE.md` 等价：

1. 先初始化，再工作；
2. 每次会话先读取 `0-System/about-me` 与最近记忆；
3. 记忆必须落盘到文件；
4. 对外动作与高风险动作先确认；
5. 不泄露隐私，不编造事实。

详细规范请直接阅读并遵守 `CLAUDE.md`。
