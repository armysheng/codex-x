{
  "name": "codex-x",
  "private": true,
  "version": "0.1.4",
  "description": "Memory-first Codex workspace toolkit with daily memory automation, Feishu bridge, plugin unlock skill, and mobile access skill.",
  "type": "module",
  "bin": {
    "codex-x": "./bin/codex-x.mjs"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "demo": "node ./scripts/run-demo.mjs",
    "test": "node --test ./test/root-cli.test.mjs && npm run test --workspaces --if-present",
    "lint:names": "node ./scripts/check-redactions.mjs",
    "init:workspace": "node ./bin/codex-x.mjs init"
  }
}
